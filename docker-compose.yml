version: '3.9'

services:
    vendo-sdk-php:
        build: docker/php
        volumes:
            - ./:/var/www
        networks:
            default: ~
networks:
    default:
        name: localdev
