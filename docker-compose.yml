version: '3.9'

services:
    vendo-sdk-php:
        build: docker/php
        volumes:
            - ./:/var/www
        networks:
            default: ~
        extra_hosts:
            -   "host.docker.internal:host-gateway"
networks:
    default:
        name: localdev
